<% unless resource.errors.empty? %>
  <% byebug %>
  <script>
    <% resources.errors.full_message.each do |key, value| %>
      <% type = key.to_s.gsub('alert', 'error').gsub('notice', 'success') %>
      toastr['<%= type %>']('<%= value %>')
    <% end %>
  </script>
<% end %>